# ContractLoop
ContractLoop is a verification-first execution engine for policy-governed automation. It transforms goals into contract-driven actions, validates permissions, observes external state, verifies outcomes, and closes loops only when success is proven.
